# 엑시트몰 작업 기준

엑시트몰은 예치금 기반 B2B 폐쇄몰과 관리자 백오피스다. 고객은 승인 후 상품 구매로 보유 재고를 쌓고, 별도 엑셀 업로드로 배송대행을 요청한다. 운영자는 가입, 입금, 상품, 구매 승인, 배송대행, 입고, CS 문의를 처리한다.

## 문서와 영역

```
.
├── CLAUDE.md -> Claude 작업 시작점
├── AGENTS.md -> Codex 작업 시작점
├── docs/
│   ├── architecture.md -> 시스템 구성과 데이터 흐름
│   ├── business-rules.md -> 예치금, 재고, 배송대행, 입고, 문의 규칙
│   ├── security.md -> 인증, 권한, 파일 접근, 감사 정책
│   ├── standards.md -> 코드, DB, UI, 검증 기준
│   ├── engineering-notes.md -> 작업 중 자주 걸리는 함정과 대응
│   ├── operations.md -> 로컬 실행, 검증, 배포 절차
│   ├── contracts.md -> 외부 화면, API, 엑셀, 파일 계약
│   └── tracking/
│       ├── status.md -> 구현된 범위와 남은 범위
│       ├── findings.md -> 아직 풀지 못한 문제
│       └── decisions/
│           ├── index.md -> 결정 기록 목록
│           ├── 0001-deposit-approval-deduction.md -> 예치금 승인 후 차감
│           ├── 0002-separate-purchase-and-shipping.md -> 구매와 배송대행 분리
│           ├── 0003-shipping-fee-separate-settlement.md -> 배송비 별도 정산
│           ├── 0004-group2-shipping-only.md -> 2그룹 권한 분리
│           ├── 0005-no-customer-rollback-after-approval.md -> 승인 후 고객 취소 금지
│           └── 0006-current-name-based-shipping-match.md -> 현재 상품명 매칭
├── app/
│   └── AGENTS.md -> 라우트와 화면 조립 기준
├── lib/
│   └── AGENTS.md -> 업무 로직, 서버 액션, Supabase 접근 기준
├── components/
│   └── AGENTS.md -> 공용 UI와 도메인 컴포넌트 기준
├── supabase/
│   └── AGENTS.md -> DB 마이그레이션, RLS, RPC 기준
└── tests/
    └── AGENTS.md -> 단위·E2E 테스트 기준
```

## 절대 지킬 것

예치금과 재고가 함께 바뀌는 작업은 원자적으로 처리한다. 앱 코드에서 여러 테이블을 순서대로 갱신해 승인 로직을 만들지 않는다.

상품 구매와 배송대행은 다른 업무다. 상품 구매는 보유 재고 적립이고, 실제 발송은 배송대행 업로드에서 처리한다.

고객 직접 취소는 검토대기 상태까지만 허용한다. 승인 이후 취소나 되돌리기 기능을 새로 만들지 않는다.

2그룹 제한은 화면, 라우트, 서버 액션에 함께 적용한다. 메뉴에서만 숨기는 방식은 권한 구현으로 보지 않는다.

비공개 파일은 권한 확인 후 링크를 발급한다. 상품 이미지 외 주문, 입고, 문의 파일을 공개 URL로 다루지 않는다.

## 작업 전 확인

예치금, 상품 구매, 배송대행, 입고, CS 문의를 바꾸기 전에는 업무 규칙과 현재 상태를 먼저 확인한다.

권한, 세션, 관리자 기능, 파일 다운로드를 바꾸기 전에는 보안 정책과 `lib/actions/_guards.ts`, `middleware.ts`의 현재 흐름을 확인한다.

DB 상태값, RLS, RPC, Storage 정책을 바꾸기 전에는 마이그레이션과 Supabase 영역 기준을 확인한다.

엑셀 양식, 상품 매칭, 송장 재업로드를 바꾸기 전에는 작업 메모의 양식·매칭 주의사항과 관련 파서 테스트를 확인한다.

화면을 바꾸기 전에는 UI 기준과 컴포넌트 영역 기준을 확인한다. 운영 화면은 정보 밀도와 반복 처리 실수 방지가 우선이다.

## 즉시 보고할 문제

고객 간 데이터나 파일이 섞일 수 있는 권한 문제를 발견하면 바로 사용자에게 보고한다.

예치금 잔액, 보유 재고, 사입재고 수량, 변동 기록이 서로 맞지 않는 변경을 발견하면 바로 멈추고 원인을 확인한다.

승인 후 고객 취소나 자동 롤백을 요구하는 변경은 현재 운영 정책과 충돌하므로 사용자 결정을 먼저 받아야 한다.

service role 키가 브라우저로 나갈 가능성, 비공개 Storage 파일 공개 가능성, RLS 우회 경로를 발견하면 바로 보고한다.

나머지 문제는 원인을 먼저 추적하고, 즉시 해결할 수 없으면 `docs/tracking/findings.md`에 조건, 영향, 막힌 이유, 가능한 접근을 남긴다.
